# Project instructions

Read JOB_APPLICATION_AGENT_MASTER_PLAN_TR.md, CODEX_EXECUTION_PROMPT_TR.md,
docs/PROJECT_STATE.md, docs/DECISIONS.md and docs/VERIFICATION.md before continuing.

The user approved local development, dependencies, synthetic browser tests and local
commits. On 2026-09-19 the user additionally explicitly authorized creating/pushing
the public caglarhekimci/job-application-agent repo and submitting the Codex for OSS
application. Do not request that publication/submission permission again merely
because the original prompt had a gate. Unknown personal form fields, any required
action-time legal terms confirmation and platform permissions remain unresolved.
Real job applications still require a concrete chosen job/package approval; paid
spending and security-sensitive account changes are not inferred from publication.

Use test-first changes; record actual RED/GREEN commands, exit codes and report paths.
Never label fixture results as model quality or live platform verification.
No personal documents from the parent directory may enter this repository.
No credentials, databases, browser sessions or real candidate data in git.
LinkedIn automation remains blocked without verified platform authorization.
Keep UI approvals separate from model-callable APIs; never expose an approval tool.
Use a local feature branch. Preserve existing work. Do not fabricate adoption or metrics.
For independent research or reviews, bounded subagents are allowed; implementation
ownership must not overlap. Local synthetic tests do not authorize real submissions.
